using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.UIElements;

public class ChoiceDio : MonoBehaviour
{
    // this is going to be diolouge that is triggered by the player pressing a button 
    // this script will be attacthed to the button acting as a "when this button i clicked do this "

    // need dio box, button variable 

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
